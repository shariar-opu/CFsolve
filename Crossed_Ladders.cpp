#include<bits/stdc++.h>
using namespace std;
#define ll long long

int x, y, c;

void solve(int tc){
    cin >> x >> y >> c;

    double l = 0, r;
}

int32_t main(){
    ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    
    int t = 1;
    cin >> t;
    for(int i = 1; i <= t; i++) solve(i);

    return 0;
}