#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define int ll
const int inf = 1e9;

void solve(int tc)
{
    string s;
    while(getline(cin, s)){
        if(s.empty()){
            cout << 0 << endl;
        }
        else cout << 1 << endl;
    }
}

int32_t main()
{
    ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    int t = 1;
    //cin >> t;
    for(int i = 1; i <= t; i++) solve(i);
}